# מעבר לאימות מאובטח — המסלול המהיר (בלי לחכות לאף אחד)

## שלב 1 — הפעלת Email/Password ב-Authentication ✅ (בוצע 28.8)

## שלב 2 — יצירת חשבונות מראש לכל המשתמשים (אתה, בקונסולה)
לכל משתמש קיים במסד (Realtime Database → Data → users) יוצרים חשבון:
Authentication → לשונית **Users** → **Add user**:
- **Email**: `<שם־המשתמש>@inv-user.app` (למשל yosef → `yosef@inv-user.app`)
- **Password**: הסיסמה הנוכחית שלו כפי שמופיעה במסד. **אם היא קצרה מ-6 תווים — מוסיפים אפסים בסוף עד לאורך 6** (למשל "1234" → "123400"). באפליקציה הם ימשיכו להקליד את הסיסמה המקורית כרגיל.

## שלב 3 — נעילת המסד (מיד אחרי שלב 2!)
Realtime Database → לשונית **Rules** → להדביק את תוכן `firebase-rules.json` → **Publish**.
אין צורך שאף אחד יתחבר לפני — החשבונות כבר קיימים, וכל אחד ימשיך להתחבר מתי שירצה בלי לשים לב לשינוי.

## שלב 4 — פריסה
`npx cap sync` + בנייה מחדש לשני המכשירים. (עד שמכשיר מקבל את הגרסה החדשה, ההתחברות הישנה בו לא תעבוד אחרי הנעילה — לכן לפרוס בהקדם.)

## הערות
- הסיסמאות הישנות שנשארו במסד הופכות אחרי הנעילה לבלתי-קריאות לכל אחד מלבד בעליהן, וכל התחברות מוצלחת מוחקת את השארית של אותו משתמש אוטומטית.
- שיתוף מחסן עם משתמש שעוד לא התחבר אף פעם מאז המעבר יציג "משתמש לא נמצא" — יעבוד אחרי ההתחברות הראשונה שלו.
- סיסמאות חדשות: מינימום 6 תווים. שינוי סיסמה — מתוך האפליקציה כרגיל.
